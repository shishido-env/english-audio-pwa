# F7: テスト更新 / README / Polish 実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** F6 完了報告で挙げた既知の制限を解消し、README に初回セットアップ手順を追加してサブプロジェクト1 を完了状態にする。

**Architecture:** README に Clerk + Neon + `.env.local` + `pnpm db:push` を含む初期セットアップを追記。`useLibrary` を request-id ガード方式で stale refetch レースを解消、mutation 失敗時は `sonner` の toast でユーザーへ通知。`afterSignOutUrl` を `ClerkProvider` 側へ移行（Clerk v7 推奨形）。

**Tech Stack:** Next.js 15 / React 19 / Clerk v7 / sonner / Vitest 4

**前提:**
- 現在のブランチは `feat/migrate-next`（F1〜F6 完了）
- 73 tests pass、typecheck/lint/build 緑
- `sonner` は既に `LibraryShell.tsx` で `<Toaster>` セット済み
- F6 完了レポートに記載の `useLibrary` レース・toast 不在・afterSignOutUrl 移行・README 不足が本フェーズの対象
- 任意項目（`/api/health` smoke test、`reviewHistory.result` CHECK 制約、`next lint` deprecation 対応、jaJP localization）は YAGNI で対象外。`next lint` deprecation は README に「将来 ESLint CLI 直接化を検討」と一行触れる程度。

---

### Task 1: README 初回セットアップ手順を追加

**Files:**
- Modify: `README.md`

**意図:** spec §13 受け入れ条件 #5「README に初回セットアップ手順（Clerk作成、Neon作成、env設定）が追記されている」を満たす。

- [x] **Step 1: 現状の README を完全置換する**

`README.md` の中身を以下に差し替える:

````markdown
# English Audio PWA

CSVでインポートした日英例文ペアを Web Speech API で読み上げる学習用 PWA。Next.js 15 + Clerk + Neon Postgres でマルチデバイス同期に対応。

## 機能

- CSV 取り込み → デッキ単位で保存（マルチデバイス同期）
- 日→英の連続読み上げ（間隔 / 音声モード切替）
- 練習モード（英語を伏せて思い出す）
- オフライン対応（PWA: アプリシェル + 既存デッキは localStorage キャッシュ）
- ダーク / ライト / システム連動テーマ

## 初回セットアップ

開発・本番デプロイともに、以下のアカウントと環境変数が必要。**個人利用 / 無料枠** での運用を想定。

### 1. 依存インストール

```bash
pnpm install
```

### 2. Clerk プロジェクト作成

1. <https://dashboard.clerk.com> でアカウント作成（無料枠 10K MAU まで）
2. **+ Create application** → 名称任意、サインイン方法は Email/Password を推奨（Google 等も追加可）
3. **API Keys** 画面で `Publishable key` と `Secret key` をコピー

### 3. Neon プロジェクト作成

**推奨: Vercel Marketplace 経由（環境変数を Vercel 側に自動セット）**

1. Vercel ダッシュボード → Storage → **Create Database** → **Neon**
2. プロジェクトを連携。`DATABASE_URL` / `DATABASE_URL_UNPOOLED` が自動で Vercel 側にセットされる
3. ローカル用に Neon ダッシュボードから接続文字列をコピーして `.env.local` に貼る

**代替: Neon 直接サインアップ**

1. <https://console.neon.tech> でアカウント作成（無料枠 0.5 GB）
2. プロジェクト作成 → Connection details から `Pooled` と `Direct` の両 URL をコピー

### 4. `.env.local` を作成

リポジトリ直下に `.env.local` を作る（Git 管理外）:

```dotenv
# Clerk
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/decks
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/decks

# Neon
DATABASE_URL=postgres://...?sslmode=require
DATABASE_URL_UNPOOLED=postgres://...?sslmode=require
```

`.env.example` のテンプレートも参考にする。

### 5. DB スキーマを作成

```bash
pnpm db:push
```

`decks` / `cards` / `review_history` テーブルが Neon 側に作成される。スキーマ変更時も同じコマンドで反映可能。

### 6. 開発サーバ起動

```bash
pnpm dev
```

`http://localhost:3000` を開き、サインインしてから `/decks` で CSV をインポートする。

## マルチデバイス同期の確認

1. 端末 A でサインイン → CSV をインポート
2. 端末 B（別ブラウザ / スマホ）で同じアカウントにサインイン
3. 端末 A のデッキが端末 B でも表示されることを確認

## CSV フォーマット

```
日本語,英語
おはよう,Good morning
ありがとう,Thank you
```

ヘッダ行（`日本語,英語` や `ja,en` 等）は自動でスキップ。1 行目から本文を書いても可。

## スクリプト

| コマンド | 用途 |
|---|---|
| `pnpm dev` | 開発サーバ（Next.js）|
| `pnpm build` | プロダクションビルド |
| `pnpm start` | ビルド済みサーバ起動 |
| `pnpm typecheck` | TypeScript 型チェック |
| `pnpm lint` | ESLint 実行（`next lint` は将来非推奨予定。CI で問題が出たら ESLint CLI へ移行する）|
| `pnpm test` | Vitest watch |
| `pnpm test:run` | Vitest 単発実行 |
| `pnpm db:push` | Drizzle Kit でスキーマを Neon に反映 |
| `pnpm db:studio` | Drizzle Studio 起動（DB ブラウザ）|

## 技術スタック

Next.js 15 (App Router) / React 19 / TypeScript / Tailwind CSS v4 / shadcn/ui / Clerk v7 / Drizzle ORM 0.38 / Neon Postgres / Web Speech API / Serwist (PWA) / Vercel

## ディレクトリ

- `app/` Next.js App Router（ルートと layout）
- `src/actions/` Server Actions（DB I/O）
- `src/db/` Drizzle スキーマとクライアント
- `src/components/` UI コンポーネント
- `src/hooks/` React フック（`useLibrary` 等）
- `src/lib/` 純粋関数とラッパ
- `src/app-shell/` アプリシェル（`LibraryShell`）

## デプロイ

GitHub と Vercel を連携し、`master` への push で自動デプロイ。Vercel 側で `NEXT_PUBLIC_CLERK_*` / `CLERK_SECRET_KEY` を環境変数として設定する（Marketplace 経由なら Neon は自動セット）。

## ライセンス / コスト

個人利用専用。Vercel Hobby / Clerk Free / Neon Free / GitHub Public の組み合わせで継続費用ゼロを維持。
````

- [x] **Step 2: 確認**

Run: `pnpm lint` (README は対象外だが副次的影響がないことを確認)
Expected: 0 errors。

- [x] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: add initial setup section (Clerk + Neon + env)"
```

---

### Task 2: useLibrary の stale refetch レース解消

**Files:**
- Modify: `src/hooks/useLibrary.ts`
- Modify: `src/hooks/useLibrary.test.ts`

**意図:** F6 で deferred になった「複数 mutation を高速連打すると最終的に古い refetch が後勝ちで上書きする可能性」を request-id ガードで解消。同時に F6 で挿入した `// eslint-disable-next-line react-hooks/set-state-in-effect` も外す。

`AbortController` は使わない（Server Actions は `signal` を受け取らないため意味なし）。代わりに `useRef` の単調増加カウンタで「最後に発火した refetch のみが setState する」方式にする。

- [x] **Step 1: テストを先に追加（failing 状態を確認）**

`src/hooks/useLibrary.test.ts` の末尾、最後の `it("空CSVは throw する...")` の **前** に以下を挿入:

```ts
  it("古い refetch は新しい refetch の結果を上書きしない", async () => {
    let resolveFirst!: (v: { ok: true; data: import("@/types").Library }) => void;
    const firstPromise = new Promise<{ ok: true; data: import("@/types").Library }>(
      (resolve) => {
        resolveFirst = resolve;
      },
    );
    mockGetLibrary.mockReturnValueOnce(firstPromise);
    mockGetLibrary.mockResolvedValueOnce({
      ok: true,
      data: {
        decks: [
          { id: "d2", name: "newer", importedAt: 2, pairs: [{ ja: "い", en: "I" }] },
        ],
        activeId: null,
      },
    });
    const { result } = renderHook(() => useLibrary());
    // 2 回目の refetch を rename 経由で発火（mockRenameDeck は success デフォルト）
    await act(async () => {
      await result.current.renameDeck("dummy", "x");
    });
    // 2 回目の結果が反映されている
    await waitFor(() =>
      expect(result.current.library.decks.map((d) => d.id)).toEqual(["d2"]),
    );
    // 古い refetch を後から解決させる
    await act(async () => {
      resolveFirst({
        ok: true,
        data: {
          decks: [
            { id: "d1", name: "older", importedAt: 1, pairs: [{ ja: "あ", en: "A" }] },
          ],
          activeId: null,
        },
      });
      await firstPromise;
    });
    // 上書きされていないこと
    expect(result.current.library.decks.map((d) => d.id)).toEqual(["d2"]);
  });
```

- [x] **Step 2: テスト失敗を確認**

Run: `pnpm vitest run src/hooks/useLibrary.test.ts`
Expected: 新しいテストが FAIL する（古い refetch が後勝ちで上書きするため）。

- [x] **Step 3: useLibrary を request-id ガード方式に変更**

`src/hooks/useLibrary.ts` を以下に置き換える:

```ts
"use client";

import { useCallback, useEffect, useRef, useState } from "react";
import type { Deck, Library } from "@/types";
import { parseCsv } from "@/lib/csv";
import {
  loadLibrary,
  saveLibrary,
  loadActiveId,
  saveActiveId,
} from "@/lib/storage";
import {
  getLibrary,
  createDeckFromCsv,
  renameDeck as renameDeckAction,
  removeDeck as removeDeckAction,
  clearAllDecks,
} from "@/actions/library";

const EMPTY_LIBRARY: Library = { decks: [], activeId: null };

function stripExtension(filename: string): string {
  return filename.replace(/\.[^.]+$/, "");
}

function mergeActiveId(library: Library, fallback: string | null): Library {
  if (library.activeId) return library;
  if (!fallback) {
    return { ...library, activeId: library.decks[0]?.id ?? null };
  }
  const exists = library.decks.some((d) => d.id === fallback);
  return { ...library, activeId: exists ? fallback : library.decks[0]?.id ?? null };
}

export function useLibrary() {
  const [library, setLibrary] = useState<Library>(() => {
    if (typeof window === "undefined") return EMPTY_LIBRARY;
    const cached = loadLibrary();
    const activeId = loadActiveId();
    return mergeActiveId(cached, activeId);
  });
  const [isLoading, setIsLoading] = useState(true);
  const requestIdRef = useRef(0);

  const refetch = useCallback(async () => {
    const myId = ++requestIdRef.current;
    const res = await getLibrary();
    if (myId !== requestIdRef.current) return;
    if (!res.ok) {
      setIsLoading(false);
      return;
    }
    const activeId = typeof window !== "undefined" ? loadActiveId() : null;
    const merged = mergeActiveId(res.data, activeId);
    setLibrary(merged);
    if (typeof window !== "undefined") {
      saveLibrary({ ...merged, activeId: null });
    }
    setIsLoading(false);
  }, []);

  useEffect(() => {
    void refetch();
  }, [refetch]);

  const activeDeck: Deck | null =
    library.decks.find((d) => d.id === library.activeId) ?? null;

  const importCsv = useCallback(
    async (filename: string, content: string) => {
      const pairs = parseCsv(content);
      if (pairs.length === 0) throw new Error("有効な行がありません");
      const name = stripExtension(filename);
      const res = await createDeckFromCsv({ name, pairs });
      if (!res.ok) throw new Error(res.error);
      await refetch();
      setLibrary((lib) => {
        const exists = lib.decks.some((d) => d.id === res.data.id);
        if (!exists) return lib;
        if (typeof window !== "undefined") saveActiveId(res.data.id);
        return { ...lib, activeId: res.data.id };
      });
    },
    [refetch],
  );

  const selectDeck = useCallback((id: string) => {
    setLibrary((lib) => {
      if (!lib.decks.some((d) => d.id === id)) return lib;
      if (typeof window !== "undefined") saveActiveId(id);
      return { ...lib, activeId: id };
    });
  }, []);

  const renameDeck = useCallback(
    async (id: string, name: string) => {
      const trimmed = name.trim();
      if (trimmed === "") return;
      const res = await renameDeckAction({ id, name });
      if (!res.ok) return;
      await refetch();
    },
    [refetch],
  );

  const removeDeck = useCallback(
    async (id: string) => {
      const res = await removeDeckAction({ id });
      if (!res.ok) return;
      const wasActive = library.activeId === id;
      await refetch();
      if (wasActive) {
        setLibrary((lib) => {
          const next = lib.decks[0]?.id ?? null;
          if (typeof window !== "undefined") saveActiveId(next);
          return { ...lib, activeId: next };
        });
      }
    },
    [library.activeId, refetch],
  );

  const clearAll = useCallback(async () => {
    const res = await clearAllDecks();
    if (!res.ok) return;
    if (typeof window !== "undefined") saveActiveId(null);
    await refetch();
  }, [refetch]);

  return {
    library,
    activeDeck,
    isLoading,
    importCsv,
    selectDeck,
    renameDeck,
    removeDeck,
    clearAll,
  };
}
```

変更点:
- `requestIdRef` を追加し、`refetch` の入口で `++requestIdRef.current` し、`getLibrary()` 解決後に自分の id が最新と一致するか確認
- 古い id のレスポンスは setState せず破棄
- `// eslint-disable-next-line react-hooks/set-state-in-effect` を削除

- [x] **Step 4: テスト緑を確認**

Run: `pnpm vitest run src/hooks/useLibrary.test.ts`
Expected: 9 tests pass（新規 1 + 既存 8）。

- [x] **Step 5: lint で eslint-disable 抜けによる回帰がないか確認**

Run: `pnpm lint`
Expected: 0 errors。`react-hooks/set-state-in-effect` warning も出ないこと（refetch 内で id 比較してから setState するため）。

- [x] **Step 6: Commit**

```bash
git add src/hooks/useLibrary.ts src/hooks/useLibrary.test.ts
git commit -m "fix(library): guard refetch against stale responses with request id"
```

---

### Task 3: mutation 失敗時の toast エラー表示

**Files:**
- Modify: `src/hooks/useLibrary.ts`
- Modify: `src/hooks/useLibrary.test.ts`
- Modify: `src/test/library-action-mocks.ts`（必要ならインライン mock の調整）

**意図:** 現状 `renameDeck` / `removeDeck` / `clearAll` は `!res.ok` を黙って捨てる。`sonner` の `toast.error` で「失敗しました」をユーザーへ通知する。`<Toaster>` は `LibraryShell.tsx` でセット済みなので追加マウントは不要。

`importCsv` は throw 経由で呼び出し側（`CsvFileInput`）の handler が toast 表示している前提（既存挙動）。本タスクでは mutation のみ対象。

- [x] **Step 1: テストで `sonner` をモック化**

`src/hooks/useLibrary.test.ts` の冒頭、既存 `import "@/test/library-action-mocks";` の **直後** に以下を追加:

```ts
import { vi } from "vitest";

const mockToastError = vi.fn();
vi.mock("sonner", () => ({
  toast: { error: mockToastError, success: vi.fn() },
}));
```

既存の `import { vi } from "vitest";` 行があれば二重 import にならないようにマージ（現状ファイルには `vi` import なし、`describe, it, expect, beforeEach` のみ）。`vi.mock` は hoist されるので位置は import 直後で OK。

- [x] **Step 2: 失敗ケースのテストを追加**

`src/hooks/useLibrary.test.ts` の末尾（最後の `it` の前）に以下を挿入:

```ts
  it("renameDeck が失敗するとエラー toast が出る", async () => {
    mockGetLibrary.mockResolvedValueOnce({
      ok: true,
      data: {
        decks: [{ id: "d1", name: "a", importedAt: 1, pairs: [{ ja: "あ", en: "A" }] }],
        activeId: null,
      },
    });
    mockRenameDeck.mockResolvedValueOnce({ ok: false, error: "not_found" });
    mockToastError.mockClear();
    const { result } = renderHook(() => useLibrary());
    await waitFor(() => expect(result.current.library.decks).toHaveLength(1));
    await act(async () => {
      await result.current.renameDeck("d1", "new");
    });
    expect(mockToastError).toHaveBeenCalledTimes(1);
    expect(mockToastError).toHaveBeenCalledWith(
      expect.stringContaining("名称変更"),
    );
  });

  it("removeDeck が失敗するとエラー toast が出る", async () => {
    mockGetLibrary.mockResolvedValueOnce({
      ok: true,
      data: {
        decks: [{ id: "d1", name: "a", importedAt: 1, pairs: [{ ja: "あ", en: "A" }] }],
        activeId: null,
      },
    });
    mockRemoveDeck.mockResolvedValueOnce({ ok: false, error: "not_found" });
    mockToastError.mockClear();
    const { result } = renderHook(() => useLibrary());
    await waitFor(() => expect(result.current.library.decks).toHaveLength(1));
    await act(async () => {
      await result.current.removeDeck("d1");
    });
    expect(mockToastError).toHaveBeenCalledTimes(1);
    expect(mockToastError).toHaveBeenCalledWith(
      expect.stringContaining("削除"),
    );
  });

  it("clearAll が失敗するとエラー toast が出る", async () => {
    mockClearAllDecks.mockResolvedValueOnce({ ok: false, error: "unauthorized" });
    mockToastError.mockClear();
    const { result } = renderHook(() => useLibrary());
    await waitFor(() => expect(result.current.isLoading).toBe(false));
    await act(async () => {
      await result.current.clearAll();
    });
    expect(mockToastError).toHaveBeenCalledTimes(1);
    expect(mockToastError).toHaveBeenCalledWith(
      expect.stringContaining("全削除"),
    );
  });
```

- [x] **Step 3: 失敗ケーステストが赤になることを確認**

Run: `pnpm vitest run src/hooks/useLibrary.test.ts`
Expected: 上記 3 テストが FAIL（まだ toast 呼び出しが無いため）。

- [x] **Step 4: `useLibrary.ts` を更新**

ファイル先頭の import に追加:

```ts
import { toast } from "sonner";
```

各 mutation の `if (!res.ok) return;` を以下に置換:

`renameDeck` 内:
```ts
      if (!res.ok) {
        toast.error("デッキの名称変更に失敗しました");
        return;
      }
```

`removeDeck` 内:
```ts
      if (!res.ok) {
        toast.error("デッキの削除に失敗しました");
        return;
      }
```

`clearAll` 内:
```ts
    if (!res.ok) {
      toast.error("デッキの全削除に失敗しました");
      return;
    }
```

`importCsv` 内の `throw new Error(res.error);` は据え置き（呼び出し側の `CsvFileInput` で toast 表示している既存挙動を維持）。

- [x] **Step 5: テスト緑を確認**

Run: `pnpm vitest run src/hooks/useLibrary.test.ts`
Expected: 12 tests pass（9 + 新規 3）。

- [x] **Step 6: 全テスト緑を確認**

Run: `pnpm test:run`
Expected: 全 76 tests pass（73 + 新規 3）。

- [x] **Step 7: Commit**

```bash
git add src/hooks/useLibrary.ts src/hooks/useLibrary.test.ts
git commit -m "feat(library): surface mutation errors via toast"
```

---

### Task 4: `afterSignOutUrl` を `ClerkProvider` 側へ移行

**Files:**
- Modify: `app/layout.tsx`
- Modify: `src/components/AppHeader.tsx`

**意図:** Clerk v7 では `afterSignOutUrl` を `ClerkProvider` で一元管理するのが推奨。現状 `UserButton` 側に prop が無く未指定（デフォルトの `/` に遷移するが、本アプリは未認証で `/` も保護されるため挙動が紛らわしい）。明示的に `/sign-in` へ戻すよう設定する。

- [x] **Step 1: `app/layout.tsx` を更新**

`<ClerkProvider>` に `afterSignOutUrl="/sign-in"` を追加:

変更前:
```tsx
    <ClerkProvider>
```

変更後:
```tsx
    <ClerkProvider afterSignOutUrl="/sign-in">
```

- [x] **Step 2: `AppHeader.tsx` の `<UserButton />` はそのまま**

すでに `afterSignOutUrl` を持っていない（プロジェクトに設定なし）ので変更不要。意図確認のみのステップ。

Run: `grep -n "afterSignOutUrl" src/components/AppHeader.tsx`
Expected: 何もマッチしない。

- [x] **Step 3: typecheck と build**

Run: `pnpm typecheck && pnpm build`
Expected: 0 errors, build success。`afterSignOutUrl` prop が `ClerkProvider` で受理されていることを TS が保証する。

- [x] **Step 4: Commit**

```bash
git add app/layout.tsx
git commit -m "chore(auth): set afterSignOutUrl on ClerkProvider"
```

---

### Task 5: 最終検証 / spec ステータス更新 / 完了レポート

**Files:**
- Modify: `docs/superpowers/specs/2026-05-28-auth-sync-foundation-design.md`
- Modify: `docs/superpowers/plans/2026-05-28-sub1-f7-polish.md`（このファイル）

- [x] **Step 1: 全自動検証**

Run:
```bash
pnpm typecheck && pnpm lint && pnpm test:run && pnpm build
```

すべて green を確認。`/decks` ルートサイズに大きな変化が無いことも目視確認する。

- [x] **Step 2: spec の Section 1 ステータス行を更新**

`docs/superpowers/specs/2026-05-28-auth-sync-foundation-design.md` の line 4:

変更前:
```
- ステータス: 実装進行中（F1〜F6 完了 / F7 次フェーズ）
```

変更後:
```
- ステータス: コード完了（F1〜F7 完了、ユーザー側手動検証残）
```

- [x] **Step 3: spec の Section 10 テーブル F7 行を更新**

変更前:
```
| F7 | `chore/test-update` | 既存テストを DB / Server Actions 対応に更新。新規 actions のテスト追加。README に初回セットアップ手順追記 | 全テスト緑、README 更新済み | 🔄 次フェーズ |
```

変更後:
```
| F7 | `chore/test-update`（実体は `feat/migrate-next` に統合） | 既存テストを DB / Server Actions 対応に更新。新規 actions のテスト追加。README に初回セットアップ手順追記。`useLibrary` レース解消 / mutation error toast / `afterSignOutUrl` 整理 | 全テスト緑、README 更新済み | ✅ コード完了（2026-05-28、全自動検証緑、ユーザー側 Neon セットアップとマルチデバイス疎通確認のみ残） |
```

- [x] **Step 4: F7 プラン（このファイル）のチェックボックスを反映**

Task 1〜5 のすべての `- [x]` を `- [x]` に変更。

- [x] **Step 5: 完了レポートをこのファイル末尾に追記**

```markdown
---

## 完了レポート（2026-05-28）

### 検証結果
- typecheck: 0 errors
- lint: 0 warnings（`next lint` deprecation 通知は据え置き、README に将来対応方針を明記）
- test:run: 76 tests pass（9 ファイル）。内訳: F6 から +3（toast 失敗ケース）+1（stale refetch ガード）= 4 件追加
- build: success。`/decks` ルートサイズに有意な変動なし

### 主な変更点
- `README.md` を全面書き換え。初回セットアップ手順（Clerk + Neon + `.env.local` + `pnpm db:push`）/ マルチデバイス検証手順 / スクリプト一覧 / ディレクトリ構成 / ライセンスとコスト
- `src/hooks/useLibrary.ts` に `requestIdRef` ガードを追加。`useEffect` 内の eslint-disable を撤去
- `useLibrary` の `renameDeck` / `removeDeck` / `clearAll` 失敗時に `sonner` の `toast.error` を発火
- `app/layout.tsx` の `<ClerkProvider>` に `afterSignOutUrl="/sign-in"` を追加
- `src/hooks/useLibrary.test.ts` に stale refetch ガードとエラー toast 3 ケースのテストを追加

### 設計上の決定事項
- request-id ガードは `AbortController` ではなく `useRef` カウンタで実装（Server Actions が `signal` を受けないため AbortController は意味なし、ref+id は React 19 でも安定）
- mutation 失敗の通知は `useLibrary` 内で完結（コンポーネント層は呼出側コードを変えずに即座に恩恵）
- `importCsv` は既存の throw 経路を維持（呼出側 `CsvFileInput` の handler が toast 担当）
- `next lint` deprecation は将来対応として README に方針記載のみ。実 ESLint CLI 化はパッケージ更新時に再評価

### 既知の制限（次サブプロジェクト以降）
- `next lint` deprecation 警告は残存。Next.js 16 以降で対応
- `/api/health` smoke test なし（実 DB 未接続のため CI で動かせない）
- `reviewHistory.result` の DB レベル CHECK 制約なし（Zod で十分と判断）
- jaJP localization は未導入（`@clerk/localizations` 軽量化のため見送り）

### ユーザーによる手動検証手順
1. Neon プロジェクトを作成し `DATABASE_URL` / `DATABASE_URL_UNPOOLED` を `.env.local` に設定
2. `pnpm db:push` で `decks` / `cards` / `review_history` テーブルを作成
3. `pnpm dev` 起動 → サインイン → CSV をインポート
4. 別の端末（または別ブラウザ）で同アカウントにサインイン
5. 同じデッキが見えることを確認 → spec §13 受け入れ条件 #2 達成

これで spec §13 の受け入れ条件 #1, #3, #4, #5, #6 はコード側で達成。#2 のみユーザー側のクラウド接続検証待ち。
```

- [x] **Step 6: 最終確認 typecheck + lint**

Run: `pnpm typecheck && pnpm lint`
Expected: 0 errors。

- [x] **Step 7: Commit**

```bash
git add docs/superpowers/specs/2026-05-28-auth-sync-foundation-design.md docs/superpowers/plans/2026-05-28-sub1-f7-polish.md
git commit -m "docs(f7): mark F7 complete and append completion report"
```

---

## ロールバック手順

すべてのタスクは独立コミット。問題発生時は該当タスクのコミットのみ `git revert` する。最悪 F6 完了状態 (`1aba707`) まで戻せる:

```bash
git reset --hard 1aba707
```

## 完了基準

- typecheck 0 errors
- lint 0 errors（`next lint` deprecation 通知は残存可）
- test:run 76 tests pass
- build success
- README に Clerk + Neon + `.env.local` + `pnpm db:push` の手順がある
- `useLibrary` に request-id ガードあり、eslint-disable なし
- mutation 失敗時に `toast.error` が発火
- `ClerkProvider` に `afterSignOutUrl` が設定されている
- spec ステータスが F7 完了に更新されている

**ユーザー側で実行が必要（受け入れ条件 #2 の最終ステップ）:**
- ⏳ Neon DB セットアップ + `pnpm db:push`
- ⏳ 2 端末で同アカウントサインイン → 同じデッキが見えることを確認

---

## 完了レポート（2026-05-28）

### 検証結果
- typecheck: 0 errors
- lint: 0 warnings（`next lint` deprecation 通知は据え置き、README に将来対応方針を明記）
- test:run: 77 tests pass（9 ファイル）。内訳: F6 から +3（toast 失敗ケース）+1（stale refetch ガード）= 4 件追加
- build: success。`/decks` ルートサイズに有意な変動なし

### 主な変更点
- `README.md` を全面書き換え。初回セットアップ手順（Clerk + Neon + `.env.local` + `pnpm db:push`）/ マルチデバイス検証手順 / スクリプト一覧 / ディレクトリ構成 / ライセンスとコスト
- `src/hooks/useLibrary.ts` に `requestIdRef` ガードを追加。`useEffect` 内の eslint-disable は eslint-plugin-react-hooks 7.x の `set-state-in-effect` rule が ref-guard を await 境界越しに pattern-match できないため、技術的制約として残存
- `useLibrary` の `renameDeck` / `removeDeck` / `clearAll` 失敗時に `sonner` の `toast.error` を発火（メッセージ「デッキの○○に失敗しました」）
- `app/layout.tsx` の `<ClerkProvider>` に `afterSignOutUrl="/sign-in"` を追加
- `src/hooks/useLibrary.test.ts` に stale refetch ガードとエラー toast 3 ケースのテストを追加。`sonner` を `vi.mock` でモック化（`vi.hoisted` で TDZ 回避）

### 設計上の決定事項
- request-id ガードは `AbortController` ではなく `useRef` カウンタで実装（Server Actions が `signal` を受けないため AbortController は意味なし、ref+id は React 19 で安定動作）
- mutation 失敗の通知は `useLibrary` 内で完結（コンポーネント層のコード変更なしで効果発現）
- `importCsv` は既存の throw 経路を維持（呼出側 `CsvFileInput` の handler が toast 担当）
- `next lint` deprecation は将来対応として README に方針記載のみ。実 ESLint CLI 化はパッケージ更新時に再評価
- `eslint-disable-next-line react-hooks/set-state-in-effect` は技術的制約により retained。race-fix は機能テストで担保

### 既知の制限（次サブプロジェクト以降）
- `next lint` deprecation 警告は残存。Next.js 16 以降で対応
- `/api/health` smoke test なし（実 DB 未接続のため CI で動かせない）
- `reviewHistory.result` の DB レベル CHECK 制約なし（Zod で十分と判断）
- jaJP localization は未導入

### ユーザーによる手動検証手順
1. Neon プロジェクトを作成し `DATABASE_URL` / `DATABASE_URL_UNPOOLED` を `.env.local` に設定
2. `pnpm db:push` で `decks` / `cards` / `review_history` テーブルを作成
3. `pnpm dev` 起動 → サインイン → CSV をインポート
4. 別の端末（または別ブラウザ）で同アカウントにサインイン
5. 同じデッキが見えることを確認 → spec §13 受け入れ条件 #2 達成

これで spec §13 の受け入れ条件 #1, #3, #4, #5, #6 はコード側で達成。#2 のみユーザー側のクラウド接続検証待ち。
